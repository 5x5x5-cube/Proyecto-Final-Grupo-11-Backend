apiVersion: apps/v1
kind: Deployment
metadata:
  name: commercial-service
  labels:
    app: commercial-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: commercial-service
  template:
    metadata:
      labels:
        app: commercial-service
    spec:
      containers:
      - name: commercial-service
        image: proyecto-final-grupo-11-backend-commercial-service:latest
        imagePullPolicy: Never
        ports:
        - containerPort: 8000
        env:
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: commercial-service-secrets
              key: database-url
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: commercial-service
spec:
  selector:
    app: commercial-service
  ports:
  - port: 80
    targetPort: 8000
