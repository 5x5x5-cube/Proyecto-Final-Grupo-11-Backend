apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-service
  labels:
    app: notification-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: notification-service
  template:
    metadata:
      labels:
        app: notification-service
    spec:
      containers:
      - name: notification-service
        image: proyecto-final-grupo-11-backend-notification-service:latest
        imagePullPolicy: Never
        ports:
        - containerPort: 8000
        env:
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: notification-service-secrets
              key: database-url
        - name: REDIS_URL
          valueFrom:
            secretKeyRef:
              name: notification-service-secrets
              key: redis-url
        - name: NOTIFICATION_QUEUE_URL
          valueFrom:
            secretKeyRef:
              name: notification-service-secrets
              key: notification-queue-url
        - name: AWS_REGION
          valueFrom:
            secretKeyRef:
              name: notification-service-secrets
              key: aws-region
        - name: AWS_ENDPOINT_URL
          valueFrom:
            secretKeyRef:
              name: notification-service-secrets
              key: aws-endpoint-url
        - name: AWS_ACCESS_KEY_ID
          value: "test"
        - name: AWS_SECRET_ACCESS_KEY
          value: "test"
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: notification-service
spec:
  selector:
    app: notification-service
  ports:
  - port: 80
    targetPort: 8000
