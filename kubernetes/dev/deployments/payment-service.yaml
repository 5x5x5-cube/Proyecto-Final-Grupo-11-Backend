apiVersion: apps/v1
kind: Deployment
metadata:
  name: payment-service
  labels:
    app: payment-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: payment-service
  template:
    metadata:
      labels:
        app: payment-service
    spec:
      containers:
      - name: payment-service
        image: proyecto-final-grupo-11-backend-payment-service:latest
        imagePullPolicy: Never
        ports:
        - containerPort: 8000
        env:
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: payment-service-secrets
              key: database-url
        - name: PAYMENT_SERVICE_URL
          valueFrom:
            secretKeyRef:
              name: payment-service-secrets
              key: payment-service-url
        - name: SNS_TOPIC_ARN
          valueFrom:
            secretKeyRef:
              name: payment-service-secrets
              key: sns-topic-arn
        - name: AWS_REGION
          valueFrom:
            secretKeyRef:
              name: payment-service-secrets
              key: aws-region
        - name: AWS_ENDPOINT_URL
          valueFrom:
            secretKeyRef:
              name: payment-service-secrets
              key: aws-endpoint-url
        - name: AWS_ACCESS_KEY_ID
          value: "test"
        - name: AWS_SECRET_ACCESS_KEY
          value: "test"
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: payment-service
spec:
  selector:
    app: payment-service
  ports:
  - port: 80
    targetPort: 8000
