apiVersion: v1
kind: ServiceAccount
metadata:
  name: search-service-sa
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::618246140762:role/proyecto-final-dev-search-service-role
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: search-service
  labels:
    app: search-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: search-service
  template:
    metadata:
      labels:
        app: search-service
    spec:
      serviceAccountName: search-service-sa
      containers:
      - name: search-service
        image: 618246140762.dkr.ecr.us-east-1.amazonaws.com/proyecto-final-dev-search-service:latest
        ports:
        - containerPort: 8000
        env:
        - name: REDIS_URL
          valueFrom:
            configMapKeyRef:
              name: shared-infra-config
              key: redis-url
        - name: SQS_QUEUE_URL
          valueFrom:
            configMapKeyRef:
              name: shared-infra-config
              key: sqs-queue-url
        - name: AWS_REGION
          valueFrom:
            configMapKeyRef:
              name: shared-infra-config
              key: aws-region
        - name: AWS_ENDPOINT_URL
          valueFrom:
            configMapKeyRef:
              name: shared-infra-config
              key: aws-endpoint-url
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 5
          periodSeconds: 5
        resources:
          requests:
            memory: "64Mi"
            cpu: "50m"
          limits:
            memory: "192Mi"
            cpu: "250m"
---
apiVersion: v1
kind: Service
metadata:
  name: search-service
spec:
  selector:
    app: search-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: ClusterIP
