apiVersion: v1
kind: ServiceAccount
metadata:
  name: search-service-sa
  annotations:
    eks.amazonaws.com/role-arn: ${SEARCH_SERVICE_ROLE_ARN}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: search-service
  labels:
    app: search-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: search-service
  template:
    metadata:
      labels:
        app: search-service
    spec:
      serviceAccountName: search-service-sa
      containers:
      - name: search-service
        image: ${ECR_REGISTRY}/search-service:${IMAGE_TAG}
        ports:
        - containerPort: 8000
        env:
        - name: REDIS_URL
          valueFrom:
            configMapKeyRef:
              name: search-service-config
              key: redis-url
        - name: SQS_QUEUE_URL
          valueFrom:
            configMapKeyRef:
              name: search-service-config
              key: sqs-queue-url
        - name: AWS_REGION
          value: "us-east-1"
        - name: AWS_ENDPOINT_URL
          value: ""
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 5
          periodSeconds: 5
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: search-service
spec:
  selector:
    app: search-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: ClusterIP
