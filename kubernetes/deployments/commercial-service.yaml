apiVersion: apps/v1
kind: Deployment
metadata:
  name: commercial-service
  labels:
    app: commercial-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: commercial-service
  template:
    metadata:
      labels:
        app: commercial-service
    spec:
      containers:
      - name: commercial-service
        image: 618246140762.dkr.ecr.us-east-1.amazonaws.com/proyecto-final-dev-commercial-service:latest
        ports:
        - containerPort: 8000
        env:
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: commercial-service-secrets
              key: database-url
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 5
          periodSeconds: 5
        resources:
          requests:
            memory: "64Mi"
            cpu: "50m"
          limits:
            memory: "128Mi"
            cpu: "130m"
---
apiVersion: v1
kind: Service
metadata:
  name: commercial-service
spec:
  selector:
    app: commercial-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: ClusterIP

